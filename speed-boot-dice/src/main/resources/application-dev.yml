game-frame:
  scan: com.simple.speedbootdice
  game-id: 1
  enable-link-logic: true

#websocket:
#  url: https://localhost/speedBoot

spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
  application:
    name: speed-boot
  main:
    allow-circular-references: true

dubbo:
  application:
    name: speed-boot
  protocol:
    name: dubbo
    port: -1
  registry:
    address: spring-cloud://nacos
    check: false
  scan:
    base-packages: com.simple.gameframe.service.impl
  consumer:
    check: false
    timeout: 60000
    retries: 0
  cloud:
    subscribed-services: game-service