package com.simple.gameframe.common;

public enum Command {

    CREATE,
    SEAT_DOWN,
    START_GAME,
    ASK_DICE,
    ANSWER_DICE,
    SELECT_SCORE,
    GAME_OVER,
    TIME_OUT,
    RECONNECT,
    DISMISS_ROOM,
    TURN_NEXT,
    DISCONNECT,
    CONNECT,
}
